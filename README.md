# @agentspyglass/core

Shared wire event types for AgentSpyglass.

Source-only TypeScript — no build step. Consumers import directly from `src/index.ts`.

## Types

- `Event`, `AgentEvent`, `ToolEvent`, `StatusEvent`, `MessageEvent`, `EventType`
- `SessionHold`
